name: "Catch"
description: "Catch a Pokemon"
inputs:
  title:
    description: "Github issue title"
    required: true
  user:
    description: "Github user"
    required: true
runs:
  using: "node16"
  main: "index.js"
